name: ProtParam

#Directory where to find the log files. Use a symbolic linnk
logDirectory: symbolic-link-to-log-directory/*.log

#Directory where to output or read the parquet file
parquetFile: symbolic-link-to-parquet-directory/protparam

#The first level path filter
firstLevelPathFilter: /protparam
